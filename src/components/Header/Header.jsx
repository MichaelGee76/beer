import "./Header.css";
import { Link, NavLink } from "react-router-dom";
import Burger from "../Burger/Burger";

const Header = () => {
    return (
        <div>
            <Burger />
        </div>
    );
};

export default Header;
